import { NextPage } from "next";

const TheFooter: NextPage = ({}) => {
  return (
    <footer className="flex items-center justify-center bg-gray-200">
      <h1>Trinity&copy;</h1>
    </footer>
  );
};

export default TheFooter;
